---
clusterDomain: "<wildcard-dns-domain>"

controlPlane:
  image: cnvrg/app:v3.12.17
  baseConfig:
    featureFlags:
      CNVRG_MOUNT_HOST_FOLDERS: true

registry:
  user: <registry-user>
  password: <registry-password>

