---
clusterDomain: "<wildcard-dns-domain>"

controlPlane:
  image: cnvrg/app:v4.7.90
  baseConfig:
    featureFlags:
      CNVRG_MOUNT_HOST_FOLDERS: false

registry:
  user: <registry-user>
  password: <registry-password>

